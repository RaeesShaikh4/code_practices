
import 'dart:async';

import 'package:flutter/material.dart';

class RxDartApp extends StatelessWidget {

  const RxDartApp({super.key});
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'RxDart Practice',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
    );
  }
}
